# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json

name: Verify & Update Codegen

permissions:
  contents: write  # Allow pushes to the repo

on:
  pull_request:
    branches:
      - 'main'
      - 'release*'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  required:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0            # Ensure full history is fetched
          persist-credentials: true  # Allow push using default token

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache-dependency-path: go.sum

      - name: Run Codegen
        run: |
          set -e
          make codegen

      - name: Commit Generated Code (if any changes)
        run: |
          # Check if there are any uncommitted changes
          if [ -n "$(git status --porcelain)" ]; then
            echo "Codegen produced changes, committing and pushing..."
            # Configure Git identity for the commit
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git config user.name "github-actions[bot]"
            git add .
            git commit -m "chore: update generated code"
            git push
          else
            echo "No generated code changes found."
          fi

      - name: Verify Generated Code Is Up To Date
        run: |
          set -e
          make verify-codegen
